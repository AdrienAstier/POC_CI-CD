name: CI & Flyway Migration

on:
  push:
    branches: ["main"]

jobs:
  migrate:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: poc_liq
        options: >-
          --health-cmd="mysqladmin ping -h localhost -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Flyway CLI
        run: |
          curl -L https://github.com/flyway/flyway/releases/download/flyway-11.12.0/flyway-commandline-11.12.0-linux-x64.tar.gz -o flyway.tar.gz
          tar -xzf flyway.tar.gz
          sudo ln -s $PWD/flyway-11.12.0/flyway /usr/local/bin/flyway

      - name: Flyway version
        run: flyway -v

      - name: Run Flyway migrations
        run: flyway -url=jdbc:mysql://localhost:3306/poc_liq -user=root -password=root -locations=filesystem:./sql migrate
